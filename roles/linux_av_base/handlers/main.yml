---
- name: Update dconf database
  ansible.builtin.command: dconf update
  changed_when: true

- name: Reboot required
  ansible.builtin.debug:
    msg: "A reboot is required before GDM autologin changes take effect."

